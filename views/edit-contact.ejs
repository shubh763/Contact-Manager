<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Contact</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1 class="animated fadeInDown">Edit Contact</h1>
        
        <% if (error) { %>
            <div class="alert alert-error animated shake"><%= error %></div>
        <% } %>
        
        <form action="/update-contact/<%= contact.id %>" method="POST" enctype="multipart/form-data" class="contact-form animated fadeInUp">
            <div class="form-group">
                <label for="profile_image">Profile Image</label>
                <div class="image-upload">
                    <label for="profile_image" class="upload-btn">
                        <i class="fas fa-camera"></i> Change Image
                    </label>
                    <input type="file" id="profile_image" name="profile_image" accept="image/*">
                    <div class="image-preview" id="imagePreview">
                        <% if (contact.profile_image) { %>
                            <img src="<%= contact.profile_image %>" alt="Current Profile Image">
                            <input type="hidden" name="existing_image" value="<%= contact.profile_image %>">
                        <% } else { %>
                            <div class="default-avatar"><%= contact.name.charAt(0).toUpperCase() %></div>
                        <% } %>
                    </div>
                </div>
            </div>
            
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" value="<%= contact.name %>" required>
            </div>
            
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" value="<%= contact.email || '' %>">
            </div>
            
            <div class="form-group">
                <label for="phone">Phone Number</label>
                <input type="tel" id="phone" name="phone" value="<%= contact.phone || '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="address">Address</label>
                <textarea id="address" name="address" rows="3"><%= contact.address || '' %></textarea>
            </div>
            
            <div class="form-actions">
                <button type="button" class="btn btn-secondary" onclick="window.location.href='/view-contacts'">Cancel</button>
                <button type="submit" class="btn btn-primary">Update Contact</button>
            </div>
        </form>
    </div>
    <script src="/js/script.js"></script>
</body>
</html>